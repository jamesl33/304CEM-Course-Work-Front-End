import { user } from './user'
import { recipe } from './recipe'
import { comments } from './comments'

const services = {
    user: user,
    recipe: recipe,
    comments: comments
}

export { services }
